# Omerta Infrastructure Environment Variables
# Copy this file to .env and fill in your values
# Then source it before running Terraform: source .env
#
# NEVER commit .env to version control!

# =============================================================================
# AWS Credentials
# =============================================================================
# Create an IAM user with these permissions:
# - EC2: Full access (or scoped to instances, security groups, EIPs)
# - Route53: Full access (or scoped to hosted zones, records)
# - (Optional) S3: For Terraform remote state

export AWS_ACCESS_KEY_ID="your-access-key-id"
export AWS_SECRET_ACCESS_KEY="your-secret-access-key"
export AWS_REGION="us-east-1"

# =============================================================================
# Terraform Variables
# =============================================================================
# These are passed to Terraform via TF_VAR_ prefix

# SSH key pair name (must exist in AWS)
export TF_VAR_key_name="your-ec2-keypair-name"

# Restrict SSH access to your IP (recommended)
# Find your IP: curl -s ifconfig.me
export TF_VAR_ssh_cidr_blocks='["YOUR_IP/32"]'

# Optional: Override defaults
# export TF_VAR_aws_region="us-west-2"
# export TF_VAR_instance_type="t3.micro"
# export TF_VAR_domain_name="omerta.run"

# Optional: Bandwidth alerts (get notified if usage spikes)
# export TF_VAR_alert_email="your-email@example.com"
# export TF_VAR_bandwidth_cap_gb="10"

# =============================================================================
# Omerta Network
# =============================================================================
# Create the network locally first:
#   omerta network create --name "omerta-prod" \
#     --bootstrap "rendezvous1.omerta.run:9999" \
#     --bootstrap "rendezvous2.omerta.run:9999"
#
# Then paste the omerta://join/... link here:
export TF_VAR_omerta_network_link="omerta://join/..."
